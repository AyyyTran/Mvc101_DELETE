@* @using System.Diagnostics *@
@model System.Diagnostics.Process[]
@{
    ViewData["Title"] = "Process Page";
    @* var procs = ViewBag.Procs as Process[]; *@
}

@* <div>
    <h2>
        @ViewData["Title"]
    </h2>
    <ul>
        @foreach(var item in procs) {
            <li>
                if(item.ProcessName.Trim().Length > 0){
                    @item.ProcessName;
                }
            </li>
        }
    </ul>
</div> *@

<div>
    <h2>
        @ViewData["Title"]
    </h2>
    <ul>
        @foreach(var item in Model) {
            <li><a asp-controller="Process"
                asp-action="Display"
                asp-route-id="@item.Id">@item.ProcessName</a>
            </li>
        }
    </ul>
</div>

